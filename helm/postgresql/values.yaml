replicaCount: 1

image:
  repository: postgres
  tag: "16"

service:
  type: ClusterIP
  port: 5432

postgres:
  db: appdb
  user: admin
  password: admin123

persistence:
  enabled: true
  storageClass: local-path
  size: 2Gi
